export type exploreWorld = {
  id: string;
  imgUrl: string;
  title: string;
};

export type newFeature = {
  imgUrl: string;
  title: string;
  subtitle: string;
};

export type social = {
  name: string;
  url: string;
  link: string;
};
